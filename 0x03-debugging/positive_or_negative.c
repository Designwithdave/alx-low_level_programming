#include "main.h"
/**
* positve_or_negative - checks for positve or negative numbers
*
*@i:the number to the checked
*
*Return: always 0
*/
void positive_or_negative(int i)
{
if (i < 0)
printf("%d id positive\n", i);
else
printf("%d is positive\n", i);
}
